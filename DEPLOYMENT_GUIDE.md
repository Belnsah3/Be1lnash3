# 🚀 Руководство по деплою LumeAI

## ✅ Что уже сделано:

1. ✅ Код загружен на GitHub: https://github.com/Belnsah3/Be1lnash3
2. ✅ Сервер обновлен с новым UI
3. ✅ Приложение перезапущено

## 📋 Быстрый деплой (3 шага):

### **Вариант 1: Автоматический деплой (рекомендуется)**

```powershell
# Одна команда для всего:
.\deploy.ps1 "Описание изменений"
```

Затем обнови сервер:
```powershell
.\update-server.ps1
```

### **Вариант 2: Ручной деплой**

```powershell
# 1. Добавить изменения
git add .

# 2. Создать коммит
git commit -m "Описание изменений"

# 3. Загрузить на GitHub
git push

# 4. Обновить сервер
ssh root@147.45.48.64
cd ~/rest-api
git pull
pm2 restart lumeai
```

## 🔧 Первоначальная настройка (один раз):

### **1. Исправить Git tracking на сервере:**

```powershell
.\fix-server-git.ps1
```

Или вручную:
```bash
ssh root@147.45.48.64
cd ~/rest-api
git branch --set-upstream-to=origin/main main
```

### **2. Проверить что все работает:**

```bash
ssh root@147.45.48.64
cd ~/rest-api
git pull  # Должно работать без ошибок
pm2 status  # Должен показать lumeai online
```

## 📊 Проверка деплоя:

### **1. Проверить статус на сервере:**

```bash
ssh root@147.45.48.64
pm2 status
pm2 logs lumeai --lines 50
```

### **2. Проверить сайт:**

- **Главная:** https://lumeai.ru
- **Dashboard:** https://lumeai.ru/dashboard
- **Login:** https://lumeai.ru/login
- **Chat:** https://lumeai.ru/chat
- **Models:** https://lumeai.ru/models
- **Activity:** https://lumeai.ru/activity
- **Keys:** https://lumeai.ru/keys

### **3. Проверить API:**

```bash
curl https://lumeai.ru/api/health
```

## 🐛 Решение проблем:

### **Проблема: "There is no tracking information"**

**Решение:**
```powershell
.\fix-server-git.ps1
```

### **Проблема: "Permission denied"**

**Решение:**
```bash
# На сервере
cd ~/rest-api
sudo chown -R root:root .
chmod -R 755 .
```

### **Проблема: "PM2 not found"**

**Решение:**
```bash
ssh root@147.45.48.64
npm install -g pm2
cd ~/rest-api
pm2 start src/server.js --name lumeai
pm2 save
pm2 startup
```

### **Проблема: Сайт не открывается**

**Проверь:**
```bash
ssh root@147.45.48.64

# 1. Статус приложения
pm2 status

# 2. Логи
pm2 logs lumeai --lines 100

# 3. Порт
netstat -tulpn | grep 3000

# 4. Nginx
sudo systemctl status nginx
sudo nginx -t
```

### **Проблема: Старый дизайн после деплоя**

**Решение:**
```bash
# Очистить кэш браузера: Ctrl+Shift+R
# Или
# Проверить что файлы обновились на сервере:
ssh root@147.45.48.64
cd ~/rest-api/public
ls -la *.html
```

## 📝 Структура проекта на сервере:

```
~/rest-api/
├── public/
│   ├── dashboard.html  ← Новый дизайн
│   ├── login.html      ← Новый дизайн
│   ├── chat.html       ← Новый дизайн
│   ├── models.html     ← Новая страница
│   ├── activity.html   ← Новая страница
│   ├── keys.html       ← Новая страница
│   └── css/
├── src/
│   ├── server.js
│   └── routes/
├── data/
│   └── api.db
├── package.json
└── .env
```

## 🔄 Workflow для ежедневной работы:

```powershell
# 1. Утром - обнови локальный код
git pull

# 2. Работай над проектом
# ... редактируй файлы ...

# 3. Сохрани и загрузи изменения
.\deploy.ps1 "Добавил новую фичу"

# 4. Обнови сервер
.\update-server.ps1

# 5. Проверь сайт
# Открой https://lumeai.ru
```

## 🎯 Полезные команды:

### **Git:**
```powershell
git status              # Посмотреть изменения
git log --oneline       # История коммитов
git diff               # Посмотреть что изменилось
```

### **Сервер:**
```bash
ssh root@147.45.48.64
pm2 status             # Статус приложений
pm2 logs lumeai        # Логи в реальном времени
pm2 restart lumeai     # Перезапустить
pm2 stop lumeai        # Остановить
pm2 start lumeai       # Запустить
```

### **Nginx:**
```bash
sudo systemctl status nginx    # Статус
sudo systemctl restart nginx   # Перезапустить
sudo nginx -t                  # Проверить конфигурацию
```

## 📚 Ссылки:

- **GitHub репозиторий:** https://github.com/Belnsah3/Be1lnash3
- **Сайт:** https://lumeai.ru
- **Сервер:** root@147.45.48.64

## ✅ Чеклист после деплоя:

- [ ] Код загружен на GitHub
- [ ] Сервер обновлен
- [ ] PM2 показывает "online"
- [ ] Сайт открывается
- [ ] Новый дизайн отображается
- [ ] Все страницы работают
- [ ] API отвечает

---

## 🎉 Готово!

Теперь у тебя:
- ✅ Современный UI в стиле OpenRouter.ai
- ✅ Автоматический деплой через скрипты
- ✅ Код на GitHub с версионностью
- ✅ Работающий сервер

**Используй `.\deploy.ps1` для быстрого деплоя!**
