# 🔧 **Исправление ошибки SQLite3 на Ubuntu**

## ❌ **Ошибка:**

```
Error: /root/rest-api/node_modules/sqlite3/build/Release/node_sqlite3.node: invalid ELF header
```

## 💡 **Причина:**

SQLite3 был скомпилирован для другой архитектуры (Windows). Нужно пересобрать native модули для Ubuntu.

---

## ✅ **Решение 1: Быстрое исправление**

### **Выполни в терминале:**

```bash
cd ~/rest-api
npm rebuild
npm start
```

---

## ✅ **Решение 2: Полная переустановка**

### **Если rebuild не помог:**

```bash
cd ~/rest-api

# Удаляем node_modules
rm -rf node_modules package-lock.json

# Устанавливаем build-essential (если еще не установлен)
sudo apt-get update
sudo apt-get install -y build-essential python3 python3-pip make g++

# Переустанавливаем зависимости
npm install

# Пересобираем native модули
npm rebuild

# Запускаем сервер
npm start
```

---

## ✅ **Решение 3: Используй обновленный скрипт**

### **Скачай обновленные файлы:**

Я обновил скрипты установки. Теперь они автоматически:
- ✅ Устанавливают build-essential
- ✅ Устанавливают python3
- ✅ Пересобирают native модули

### **Запусти снова:**

```bash
cd ~/rest-api
./install.sh
```

Или:

```bash
cd ~/rest-api
python3 setup_ubuntu.py
```

---

## 🔍 **Проверка:**

### **После исправления проверь:**

```bash
# Проверь что Node.js работает
node --version

# Проверь что SQLite3 собран правильно
npm list sqlite3

# Запусти сервер
npm start
```

### **Ожидаемый результат:**

```
✅ База данных инициализирована
🚀 Сервер запущен на порту 3000
📚 Документация доступна по адресу: http://147.45.48.64:3000/api-docs
```

---

## 📋 **Что было исправлено в скриптах:**

### **install.sh:**
```bash
# Добавлено:
sudo apt-get install -y build-essential python3 python3-pip make g++
npm rebuild
```

### **setup_ubuntu.py:**
```python
# Добавлено:
run_command("npm rebuild", "Пересборка native модулей")
```

---

## 🆘 **Если все еще не работает:**

### **1. Проверь версию Node.js:**

```bash
node --version
```

Должна быть **v20.x.x** или выше.

### **2. Проверь наличие build-essential:**

```bash
dpkg -l | grep build-essential
```

Если не установлен:

```bash
sudo apt-get install -y build-essential
```

### **3. Проверь права доступа:**

```bash
ls -la ~/rest-api/node_modules/sqlite3/
```

Если проблемы с правами:

```bash
sudo chown -R $USER:$USER ~/rest-api
```

### **4. Попробуй установить sqlite3 отдельно:**

```bash
npm install sqlite3 --build-from-source
```

---

## ✨ **Готово!**

После выполнения одного из решений сервер должен запуститься без ошибок.

**Запусти:**
```bash
npm start
```

**Открой:**
```
http://your-server-ip:3000
```

---

## 📞 **Нужна помощь?**

Если проблема сохраняется:

1. Покажи вывод команды: `npm rebuild`
2. Покажи вывод команды: `node --version`
3. Покажи вывод команды: `npm list sqlite3`

---

**Теперь SQLite3 будет работать на Ubuntu!** 🚀
