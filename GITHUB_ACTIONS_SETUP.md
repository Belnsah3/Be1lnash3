# 🔐 **Настройка GitHub Actions для автодеплоя**

## ⚠️ **ВАЖНО: Безопасность!**

**НИКОГДА не используй пароль в GitHub Actions!**  
Используй SSH ключ для безопасного подключения.

---

## 🔧 **Настройка SSH ключа (один раз):**

### **Шаг 1: Создай SSH ключ на сервере**

```bash
ssh YOUR_USERNAME@YOUR_SERVER_IP
# Введи пароль

# Создай SSH ключ для GitHub Actions
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions -N ""

# Добавь публичный ключ в authorized_keys
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys

# Установи правильные права
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh

# Скопируй ПРИВАТНЫЙ ключ (для GitHub)
cat ~/.ssh/github_actions
```

**Скопируй весь вывод последней команды (включая BEGIN и END)**

---

### **Шаг 2: Добавь секреты на GitHub**

1. Открой: https://github.com/Belnsah3/Be1lnash3/settings/secrets/actions
2. Нажми **"New repository secret"**

**Добавь 3 секрета:**

#### **Секрет 1: SERVER_HOST**
- Name: `SERVER_HOST`
- Value: `YOUR_SERVER_IP`

#### **Секрет 2: SERVER_USER**
- Name: `SERVER_USER`
- Value: `YOUR_USERNAME`

#### **Секрет 3: SERVER_SSH_KEY**
- Name: `SERVER_SSH_KEY`
- Value: (вставь скопированный приватный ключ из `cat ~/.ssh/github_actions`)

---

## ✅ **После настройки:**

GitHub Actions будет автоматически деплоить при каждом `git push`!

**Проверь:** https://github.com/Belnsah3/Be1lnash3/actions

---

## 🔒 **Почему SSH ключ безопаснее пароля:**

1. ✅ Ключ можно отозвать без смены пароля
2. ✅ Ключ работает только с одного места (GitHub)
3. ✅ Ключ не виден в логах
4. ✅ Ключ нельзя подобрать

**Пароль в коде = ОПАСНО!** ❌

---

## 📋 **Команды для настройки:**

```bash
# На сервере
ssh YOUR_USERNAME@YOUR_SERVER_IP

# Создай ключ
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions -N ""

# Добавь в authorized_keys
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys

# Права
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh

# Покажи приватный ключ (скопируй для GitHub)
cat ~/.ssh/github_actions
```

---

## 🎉 **Готово!**

После настройки каждый `git push` будет автоматически обновлять сервер!
