# 📋 **Git Шпаргалка для LumeAI**

## ⚡ **Самые частые команды:**

```powershell
# 🚀 Полный деплой (GitHub + сервер)
.\deploy.ps1 "Описание изменений"

# ⚡ Быстрый коммит (только GitHub)
.\quick-commit.ps1 "Описание"

# 📊 Проверить статус
git status

# 📥 Скачать изменения
git pull

# 📝 История коммитов
git log --oneline
```

---

## 📝 **Базовый workflow:**

```bash
# 1️⃣ Проверить что изменилось
git status

# 2️⃣ Добавить файлы
git add .

# 3️⃣ Создать коммит
git commit -m "Описание"

# 4️⃣ Загрузить на GitHub
git push
```

---

## 🔄 **Работа с изменениями:**

```bash
# Посмотреть изменения
git diff

# Посмотреть изменения в конкретном файле
git diff filename.js

# Отменить изменения в файле
git checkout -- filename.js

# Отменить все изменения
git reset --hard

# Отменить последний коммит (оставить изменения)
git reset --soft HEAD~1
```

---

## 🌿 **Работа с ветками:**

```bash
# Создать новую ветку
git checkout -b feature-name

# Переключиться на ветку
git checkout main

# Посмотреть все ветки
git branch

# Слить ветку в текущую
git merge feature-name

# Удалить ветку
git branch -d feature-name
```

---

## 📡 **Работа с сервером:**

```bash
# Обновить код на сервере
ssh root@147.45.48.64 "cd ~/rest-api && git pull && ./restart.sh app"

# Посмотреть логи на сервере
ssh root@147.45.48.64 "pm2 logs lumeai --lines 50"

# Проверить статус на сервере
ssh root@147.45.48.64 "cd ~/rest-api && git status"
```

---

## 🔍 **Просмотр истории:**

```bash
# История коммитов
git log

# Красивая история
git log --oneline --graph --all

# Последние 10 коммитов
git log --oneline -10

# Изменения в коммите
git show <commit-hash>

# Кто изменял файл
git blame filename.js
```

---

## 🔧 **Настройка:**

```bash
# Посмотреть настройки
git config --list

# Изменить имя
git config --global user.name "Belnash3"

# Изменить email
git config --global user.email "sahsaxboxvanx@gmail.com"

# Посмотреть remote
git remote -v

# Изменить remote URL
git remote set-url origin https://github.com/Belnash3/LumeAI.git
```

---

## 🆘 **Экстренные случаи:**

```bash
# Сохранить изменения временно
git stash

# Вернуть сохраненные изменения
git stash pop

# Посмотреть список сохраненных изменений
git stash list

# Откатиться к конкретному коммиту
git reset --hard <commit-hash>

# Отменить коммит (создать новый коммит с отменой)
git revert <commit-hash>
```

---

## 🐛 **Решение конфликтов:**

```bash
# 1. Посмотреть конфликты
git status

# 2. Открыть файлы с конфликтами
# Найти строки:
# <<<<<<< HEAD
# твои изменения
# =======
# изменения с сервера
# >>>>>>> branch-name

# 3. Исправить вручную, удалив маркеры

# 4. Добавить исправленные файлы
git add .

# 5. Завершить merge
git commit -m "Resolve merge conflict"
```

---

## 📊 **Полезные алиасы:**

Добавь в `~/.gitconfig`:

```ini
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    lg = log --oneline --graph --all
    last = log -1 HEAD
    unstage = reset HEAD --
```

Теперь можно использовать:

```bash
git st      # вместо git status
git co main # вместо git checkout main
git lg      # красивая история
```

---

## 🎯 **Примеры коммитов:**

```bash
# Новая функция
git commit -m "Add chat page with file upload"

# Исправление бага
git commit -m "Fix login validation error"

# Обновление стилей
git commit -m "Update dashboard mobile styles"

# Рефакторинг
git commit -m "Refactor auth routes"

# Документация
git commit -m "Update README with new features"

# Сессия завершена
git commit -m "Session 1 complete: Registration + Login"
```

---

## 📁 **Работа с файлами:**

```bash
# Удалить файл из Git (но оставить локально)
git rm --cached filename.js

# Переименовать файл
git mv old-name.js new-name.js

# Добавить конкретный файл
git add filename.js

# Добавить все .js файлы
git add *.js

# Добавить все файлы в папке
git add public/
```

---

## 🔐 **Безопасность:**

```bash
# Проверить что в .gitignore
cat .gitignore

# Посмотреть что будет закоммичено
git status

# Убедиться что база данных не добавлена
git status | grep .db

# Удалить файл из истории (если случайно закоммитил)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch data/api.db" \
  --prune-empty --tag-name-filter cat -- --all
```

---

## 🚀 **Быстрые команды:**

```bash
# Всё в одной строке
git add . && git commit -m "Quick update" && git push

# Коммит всех изменений (без git add)
git commit -am "Update"

# Изменить последний коммит
git commit --amend -m "New message"

# Добавить файлы к последнему коммиту
git add . && git commit --amend --no-edit
```

---

## 📞 **Помощь:**

```bash
# Справка по команде
git help <command>

# Например
git help commit
git help push
git help merge

# Краткая справка
git <command> -h
```

---

## ✅ **Ежедневный чеклист:**

```bash
# Утро
git pull                              # Скачать изменения

# Работа
# ... редактируешь файлы ...

# Сохранение
git status                            # Проверить изменения
.\deploy.ps1 "Описание"              # Загрузить на GitHub + сервер

# Или вручную:
git add .                             # Добавить файлы
git commit -m "Описание"              # Создать коммит
git push                              # Загрузить на GitHub
ssh root@147.45.48.64 "cd ~/rest-api && git pull && ./restart.sh app"  # Обновить сервер
```

---

## 🎉 **Готово!**

**Сохрани эту шпаргалку!**

Самые важные команды:
- `.\deploy.ps1 "message"` - для деплоя
- `git status` - проверить статус
- `git pull` - скачать изменения
- `git log --oneline` - история

**Удачи!** 🚀
