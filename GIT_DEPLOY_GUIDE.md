# 🚀 **Руководство по Git и деплою на сервер**

## 📋 **Содержание:**
1. [Настройка Git](#1-настройка-git)
2. [Создание репозитория на GitHub](#2-создание-репозитория-на-github)
3. [Первая загрузка проекта](#3-первая-загрузка-проекта)
4. [Ежедневная работа](#4-ежедневная-работа)
5. [Автоматический деплой](#5-автоматический-деплой)
6. [Полезные команды](#6-полезные-команды)

---

## 1. Настройка Git

### **Шаг 1: Установка Git (если еще не установлен)**

**Windows:**
```bash
# Скачай и установи с https://git-scm.com/download/win
# Или через winget:
winget install Git.Git
```

**Проверка установки:**
```bash
git --version
```

### **Шаг 2: Настройка Git**

```bash
# Открой PowerShell в папке проекта (d:\bukkit\rest-api)
cd d:\bukkit\rest-api

# Настрой имя и email
git config --global user.name "Belnash3"
git config --global user.email "sahsaxboxvanx@gmail.com"

# Проверь настройки
git config --list
```

### **Шаг 3: Создай .gitignore файл**

Этот файл уже создан ниже, он исключает ненужные файлы из Git.

---

## 2. Создание репозитория на GitHub

### **Вариант 1: Через веб-интерфейс (как на скриншоте)**

1. Открой https://github.com/new
2. **Repository name:** `LumeAI` (или `rest-api`)
3. **Description:** `LumeAI - AI API Gateway with GPT4Free.pro integration`
4. **Visibility:** Private (рекомендую)
5. **НЕ добавляй** README, .gitignore, license (у нас уже есть)
6. Нажми **"Create repository"**

### **Вариант 2: Через GitHub CLI**

```bash
# Установи GitHub CLI
winget install GitHub.cli

# Войди в аккаунт
gh auth login

# Создай репозиторий
gh repo create LumeAI --private --source=. --remote=origin
```

---

## 3. Первая загрузка проекта

### **Шаг 1: Инициализация Git**

```bash
# Открой PowerShell в папке проекта
cd d:\bukkit\rest-api

# Инициализируй Git (если еще не сделано)
git init

# Добавь remote (замени USERNAME на твой GitHub username)
git remote add origin https://github.com/Belnash3/LumeAI.git

# Или через SSH (если настроен SSH ключ)
git remote add origin git@github.com:Belnash3/LumeAI.git
```

### **Шаг 2: Первый коммит**

```bash
# Добавь все файлы
git add .

# Создай коммит
git commit -m "Initial commit: LumeAI API Gateway"

# Создай ветку main
git branch -M main

# Загрузи на GitHub
git push -u origin main
```

### **Если просит авторизацию:**

**Windows:**
- Появится окно авторизации GitHub
- Войди через браузер
- Токен сохранится автоматически

**Или создай Personal Access Token:**
1. https://github.com/settings/tokens
2. Generate new token (classic)
3. Выбери scopes: `repo`, `workflow`
4. Скопируй токен
5. Используй вместо пароля

---

## 4. Ежедневная работа

### **Простой процесс (3 команды):**

```bash
# 1. Добавить изменения
git add .

# 2. Создать коммит с описанием
git commit -m "Описание изменений"

# 3. Загрузить на GitHub
git push
```

### **Примеры коммитов:**

```bash
git commit -m "Add new registration page"
git commit -m "Fix login validation"
git commit -m "Update dashboard styles"
git commit -m "Add chat functionality"
```

### **Проверка статуса:**

```bash
# Посмотреть что изменилось
git status

# Посмотреть изменения в файлах
git diff

# История коммитов
git log --oneline
```

---

## 5. Автоматический деплой

### **Вариант 1: Ручной деплой (простой)**

**На сервере один раз настрой:**

```bash
ssh root@147.45.48.64

# Перейди в папку проекта
cd ~/rest-api

# Настрой Git (если еще не сделано)
git config --global user.name "Server"
git config --global user.email "server@lumeai.ru"

# Подключи к GitHub
git remote add origin https://github.com/Belnash3/LumeAI.git
```

**Каждый раз для обновления:**

```bash
ssh root@147.45.48.64
cd ~/rest-api
git pull
./restart.sh app
```

### **Вариант 2: Скрипт для быстрого деплоя**

Создай файл `deploy.ps1` на локальном компьютере (уже создан ниже).

**Использование:**

```powershell
# Загрузить на GitHub и обновить на сервере
.\deploy.ps1 "Описание изменений"

# Или просто
.\deploy.ps1
```

### **Вариант 3: GitHub Actions (автоматический деплой)**

Создай файл `.github/workflows/deploy.yml` (уже создан ниже).

**Как работает:**
1. Ты делаешь `git push`
2. GitHub автоматически подключается к серверу
3. Обновляет код
4. Перезапускает приложение

---

## 6. Полезные команды

### **Отмена изменений:**

```bash
# Отменить изменения в файле
git checkout -- filename.js

# Отменить все изменения
git reset --hard

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1
```

### **Ветки:**

```bash
# Создать новую ветку
git checkout -b feature-chat

# Переключиться на ветку
git checkout main

# Слить ветку
git merge feature-chat

# Удалить ветку
git branch -d feature-chat
```

### **Обновление с сервера:**

```bash
# Скачать изменения с GitHub
git pull

# Или
git fetch
git merge origin/main
```

### **Просмотр истории:**

```bash
# История коммитов
git log

# Красивая история
git log --oneline --graph --all

# Изменения в коммите
git show <commit-hash>
```

---

## 🎯 **Рекомендуемый workflow:**

### **Ежедневная работа:**

```bash
# 1. Утром - обнови код с GitHub
git pull

# 2. Работай над проектом
# ... редактируй файлы ...

# 3. Сохрани изменения
git add .
git commit -m "Add new feature"

# 4. Загрузи на GitHub
git push

# 5. Обнови на сервере
ssh root@147.45.48.64 "cd ~/rest-api && git pull && ./restart.sh app"
```

### **Или используй скрипт deploy.ps1:**

```powershell
# Одна команда вместо всех выше
.\deploy.ps1 "Add new feature"
```

---

## 🔐 **Безопасность:**

### **Что НЕ загружать на GitHub:**

✅ Уже настроено в `.gitignore`:
- `node_modules/` - зависимости
- `data/*.db` - база данных
- `.env` - секретные ключи
- `*.log` - логи

### **Секретные данные:**

**Используй .env файл:**

```bash
# .env (НЕ загружается на GitHub)
DATABASE_URL=sqlite:./data/api.db
SESSION_SECRET=your-secret-key-here
GPT4FREE_API_KEY=your-api-key
```

**В коде используй:**

```javascript
require('dotenv').config();
const apiKey = process.env.GPT4FREE_API_KEY;
```

---

## 🐛 **Решение проблем:**

### **Проблема: "Permission denied"**

```bash
# Настрой SSH ключ
ssh-keygen -t ed25519 -C "sahsaxboxvanx@gmail.com"

# Добавь ключ на GitHub
cat ~/.ssh/id_ed25519.pub
# Скопируй и добавь на https://github.com/settings/keys
```

### **Проблема: "Merge conflict"**

```bash
# 1. Посмотри конфликты
git status

# 2. Открой файлы и исправь конфликты
# Найди строки с <<<<<<< и >>>>>>>

# 3. Добавь исправленные файлы
git add .

# 4. Заверши merge
git commit -m "Resolve merge conflict"
```

### **Проблема: "Forgot to pull before push"**

```bash
# 1. Сохрани свои изменения
git stash

# 2. Обнови с GitHub
git pull

# 3. Верни свои изменения
git stash pop

# 4. Загрузи на GitHub
git push
```

---

## 📚 **Полезные ссылки:**

- **Git документация:** https://git-scm.com/doc
- **GitHub Guides:** https://guides.github.com/
- **Git Cheat Sheet:** https://education.github.com/git-cheat-sheet-education.pdf
- **Visual Git Guide:** https://marklodato.github.io/visual-git-guide/index-ru.html

---

## ✅ **Чеклист первой настройки:**

- [ ] Установлен Git
- [ ] Настроены user.name и user.email
- [ ] Создан репозиторий на GitHub
- [ ] Создан .gitignore
- [ ] Сделан первый commit
- [ ] Код загружен на GitHub
- [ ] На сервере настроен git pull
- [ ] Создан скрипт deploy.ps1
- [ ] Протестирован деплой

---

## 🎉 **Готово!**

Теперь у тебя:
- ✅ Код на GitHub (бэкап + версионность)
- ✅ Быстрый деплой на сервер
- ✅ История всех изменений
- ✅ Возможность откатиться к любой версии

**Используй скрипт `deploy.ps1` для быстрого деплоя!**
