# ✅ **Git настроен! Инструкция по использованию**

## 📁 **Созданные файлы:**

1. ✅ `.gitignore` - Исключает ненужные файлы (база данных, node_modules, логи)
2. ✅ `deploy.ps1` - Автоматический деплой (GitHub + сервер)
3. ✅ `quick-commit.ps1` - Быстрый коммит на GitHub
4. ✅ `.github/workflows/deploy.yml` - GitHub Actions (автодеплой)
5. ✅ `GIT_DEPLOY_GUIDE.md` - Полное руководство
6. ✅ `QUICK_START_GIT.md` - Краткая инструкция

---

## 🚀 **Что делать дальше:**

### **Шаг 1: Создай репозиторий на GitHub**

1. Открой: https://github.com/new
2. **Repository name:** `LumeAI`
3. **Description:** `AI API Gateway with modern dashboard`
4. **Visibility:** ✅ Private (рекомендую)
5. **НЕ добавляй** README, .gitignore, license
6. Нажми **"Create repository"**

### **Шаг 2: Подключи проект к GitHub**

Открой PowerShell в папке проекта:

```powershell
cd d:\bukkit\rest-api

# Инициализируй Git (если еще не сделано)
git init

# Добавь remote (ЗАМЕНИ Belnash3 на свой username!)
git remote add origin https://github.com/Belnash3/LumeAI.git

# Проверь, что remote добавлен
git remote -v
```

### **Шаг 3: Первый коммит**

```powershell
# Добавь все файлы
git add .

# Создай коммит
git commit -m "Initial commit: LumeAI with new registration"

# Создай ветку main
git branch -M main

# Загрузи на GitHub
git push -u origin main
```

**При первом push:**
- Появится окно авторизации GitHub
- Войди через браузер
- Токен сохранится автоматически

---

## 📝 **Ежедневное использование:**

### **Самый простой способ:**

```powershell
# Одна команда для всего!
.\deploy.ps1 "Описание изменений"
```

**Что делает скрипт:**
1. ✅ Показывает измененные файлы
2. ✅ Добавляет все файлы в Git
3. ✅ Создает коммит
4. ✅ Загружает на GitHub
5. ✅ Спрашивает, обновить ли сервер
6. ✅ Обновляет код на сервере
7. ✅ Перезапускает приложение

**Примеры:**

```powershell
.\deploy.ps1 "Add chat page"
.\deploy.ps1 "Fix login bug"
.\deploy.ps1 "Update dashboard styles"
.\deploy.ps1 "Session 1 complete"
```

---

## 🔄 **Альтернативные способы:**

### **Вариант 1: Только GitHub (без обновления сервера)**

```powershell
.\quick-commit.ps1 "Quick update"
```

### **Вариант 2: Вручную (классический Git)**

```bash
git add .
git commit -m "Update"
git push
```

### **Вариант 3: Обновить только сервер**

```bash
ssh YOUR_USERNAME@YOUR_SERVER_IP "cd ~/rest-api && git pull && ./restart.sh app"
```

---

## 🎯 **Типичный рабочий день:**

```powershell
# 1. Утром - начало работы
cd d:\bukkit\rest-api

# 2. Работаешь над проектом
# ... редактируешь файлы ...

# 3. Сохраняешь изменения (несколько раз в день)
.\deploy.ps1 "Add feature X"

# 4. Продолжаешь работу
# ... еще изменения ...

# 5. Снова сохраняешь
.\deploy.ps1 "Fix bug Y"

# 6. Вечером - финальное сохранение
.\deploy.ps1 "End of day commit"
```

---

## 📊 **Проверка статуса:**

```bash
# Что изменилось?
git status

# Какие файлы изменены?
git diff

# История коммитов
git log --oneline

# Последние 5 коммитов
git log --oneline -5
```

---

## 🔐 **Настройка автоматического деплоя (GitHub Actions):**

### **Шаг 1: Создай SSH ключ на сервере**

```bash
ssh YOUR_USERNAME@YOUR_SERVER_IP

# Создай ключ
ssh-keygen -t ed25519 -C "github-actions"
# Нажми Enter 3 раза

# Добавь в authorized_keys
cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys

# Скопируй приватный ключ
cat ~/.ssh/id_ed25519
# Скопируй весь вывод (включая BEGIN и END)
```

### **Шаг 2: Добавь секреты на GitHub**

1. Открой: https://github.com/Belnash3/LumeAI/settings/secrets/actions
2. Нажми "New repository secret"
3. Добавь 3 секрета:

**Секрет 1:**
- Name: `SERVER_HOST`
- Value: `YOUR_SERVER_IP`

**Секрет 2:**
- Name: `SERVER_USER`
- Value: `root`

**Секрет 3:**
- Name: `SERVER_SSH_KEY`
- Value: (вставь скопированный приватный ключ)

### **Шаг 3: Готово!**

Теперь при каждом `git push` код автоматически обновится на сервере!

---

## 🐛 **Решение проблем:**

### **Проблема 1: "Permission denied" при push**

**Решение:**
```bash
# Создай Personal Access Token
# 1. Открой: https://github.com/settings/tokens
# 2. Generate new token (classic)
# 3. Выбери scopes: repo, workflow
# 4. Скопируй токен
# 5. При push используй токен вместо пароля
```

### **Проблема 2: "Remote already exists"**

**Решение:**
```bash
# Удали старый remote
git remote remove origin

# Добавь новый
git remote add origin https://github.com/Belnash3/LumeAI.git
```

### **Проблема 3: "Cannot connect to server"**

**Решение:**
```bash
# Проверь доступность сервера
ping YOUR_SERVER_IP

# Проверь SSH
ssh YOUR_USERNAME@YOUR_SERVER_IP

# Если не работает - обнови вручную через SSH
```

### **Проблема 4: "Merge conflict"**

**Решение:**
```bash
# Сохрани свои изменения
git stash

# Скачай с GitHub
git pull

# Верни свои изменения
git stash pop

# Если конфликт - открой файлы и исправь вручную
# Найди строки с <<<<<<< и >>>>>>>
```

---

## 📚 **Полезные команды:**

```bash
# Отменить изменения в файле
git checkout -- filename.js

# Отменить все изменения
git reset --hard

# Отменить последний коммит (оставить изменения)
git reset --soft HEAD~1

# Посмотреть изменения в коммите
git show <commit-hash>

# Создать ветку
git checkout -b feature-name

# Переключиться на main
git checkout main

# Слить ветку
git merge feature-name
```

---

## ✅ **Чеклист готовности:**

- [ ] Git установлен
- [ ] Настроены user.name и user.email
- [ ] Создан репозиторий на GitHub
- [ ] Проект подключен к GitHub (git remote)
- [ ] Сделан первый commit и push
- [ ] Протестирован скрипт deploy.ps1
- [ ] На сервере настроен git pull
- [ ] (Опционально) Настроен GitHub Actions

---

## 🎉 **Готово к работе!**

**Используй:**
```powershell
.\deploy.ps1 "Your message here"
```

**Это всё, что нужно!** 🚀

---

## 📞 **Нужна помощь?**

Смотри полную документацию:
- `GIT_DEPLOY_GUIDE.md` - Подробное руководство
- `QUICK_START_GIT.md` - Краткая инструкция

**Удачи с проектом!** 💪
