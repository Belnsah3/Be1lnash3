# 🖥️ NEW SERVER CONFIGURATION

**Date:** 2025-11-08  
**Status:** Configured

---

## 📋 **НОВЫЙ СЕРВЕР:**

```
IP:       192.168.31.26
Login:    be1lnash3
Password: 2246
Type:     Local network
```

---

## ✅ **ОБНОВЛЕННЫЕ СКРИПТЫ:**

### **1. quick-update.ps1** ✅
```powershell
$pass = "2246"
$server = "be1lnash3@192.168.31.26"
```
**Использование:**
```powershell
.\quick-update.ps1
```

### **2. deploy.ps1** ✅
```powershell
# Обновлена команда SSH
ssh be1lnash3@192.168.31.26 'cd ~/rest-api && git pull && pm2 restart lumeai'
```
**Использование:**
```powershell
.\deploy.ps1 -Message "Your commit message"
```

### **3. update-server.ps1** ✅
```powershell
ssh be1lnash3@192.168.31.26 @"
# ... commands
"@
```
**Использование:**
```powershell
.\update-server.ps1
```

### **4. update-server-auto.ps1** ✅
```powershell
echo y | plink -batch -pw $password be1lnash3@192.168.31.26 "bash -s" < temp_commands.sh
```
**Использование:**
```powershell
.\update-server-auto.ps1
```

---

## 🔧 **ДОПОЛНИТЕЛЬНАЯ НАСТРОЙКА:**

### **Создать файл с паролем:**
```powershell
# В корне проекта создать файл .server-password
echo "2246" > .server-password
```

**Примечание:** Файл `.server-password` в `.gitignore` и не будет закоммичен.

---

## 🚀 **БЫСТРЫЙ СТАРТ:**

### **1. Обновить сервер (с автоматическим паролем):**
```powershell
.\quick-update.ps1
```

### **2. Деплой изменений:**
```powershell
# Коммит и пуш в GitHub
.\deploy.ps1 -Message "Update feature"

# Затем обновить сервер
.\quick-update.ps1
```

### **3. Прямое подключение SSH:**
```powershell
ssh be1lnash3@192.168.31.26
# Password: 2246
```

---

## 📊 **СРАВНЕНИЕ СЕРВЕРОВ:**

| Параметр | Старый сервер | Новый сервер |
|----------|---------------|--------------|
| IP | 147.45.48.64 | 192.168.31.26 |
| Login | root | be1lnash3 |
| Password | 24162006gorA! | 2246 |
| Type | Public | Local network |
| URL | https://lumeai.ru | http://192.168.31.26 |

---

## ⚠️ **ВАЖНЫЕ ЗАМЕЧАНИЯ:**

### **1. Локальная сеть:**
- Новый сервер доступен только из локальной сети
- IP 192.168.31.26 - внутренний адрес
- Для публичного доступа нужен port forwarding или туннель

### **2. HTTP vs HTTPS:**
- Старый: https://lumeai.ru (SSL сертификат)
- Новый: http://192.168.31.26 (без SSL)

### **3. Права доступа:**
- Старый: root (полные права)
- Новый: be1lnash3 (возможно ограниченные права)

---

## 🔐 **БЕЗОПАСНОСТЬ:**

### **Рекомендации для нового сервера:**

1. **Изменить пароль на более сложный:**
   ```bash
   passwd
   ```

2. **Настроить SSH ключи:**
   ```bash
   ssh-keygen -t ed25519
   ssh-copy-id be1lnash3@192.168.31.26
   ```

3. **Настроить firewall:**
   ```bash
   sudo ufw enable
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw allow 22/tcp
   ```

4. **Обновить систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## 📝 **CHECKLIST НАСТРОЙКИ:**

### **На новом сервере нужно установить:**

- [ ] Node.js и npm
- [ ] PM2 (process manager)
- [ ] Git
- [ ] Nginx (web server)
- [ ] Python 3 и pip (для Python G4F API)
- [ ] Клонировать репозиторий rest-api
- [ ] Настроить PM2 процессы
- [ ] Настроить Nginx reverse proxy
- [ ] Настроить firewall (ufw)

### **Команды для установки:**

```bash
# Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# PM2
sudo npm install -g pm2

# Git
sudo apt install -y git

# Nginx
sudo apt install -y nginx

# Python
sudo apt install -y python3 python3-pip python3-venv

# Клонировать проект
cd ~
git clone https://github.com/Be1lnash3/rest-api.git
cd rest-api

# Установить зависимости
npm install

# Запустить через PM2
pm2 start src/server.js --name lumeai
pm2 save
pm2 startup
```

---

## ✅ **ПРОВЕРКА РАБОТЫ:**

### **1. Проверить доступность:**
```bash
curl http://192.168.31.26
```

### **2. Проверить PM2:**
```bash
pm2 list
pm2 logs lumeai
```

### **3. Проверить Nginx:**
```bash
sudo systemctl status nginx
```

### **4. Проверить порты:**
```bash
sudo netstat -tlnp | grep -E ':(80|443|3000|5000)'
```

---

## 🎯 **ИТОГ:**

### **Обновлено:**
- ✅ quick-update.ps1
- ✅ deploy.ps1
- ✅ update-server.ps1
- ✅ update-server-auto.ps1

### **Готово к использованию:**
- ✅ Все скрипты настроены на новый сервер
- ✅ Пароль и логин обновлены
- ✅ IP адрес изменен

### **Следующие шаги:**
1. Создать `.server-password` файл с паролем "2246"
2. Проверить работу скриптов
3. Настроить новый сервер (если еще не настроен)

---

**Конфигурация обновлена!** ✅  
**Готово к работе!** 🚀
