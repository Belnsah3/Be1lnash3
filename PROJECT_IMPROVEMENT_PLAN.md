# 🚀 План полной доработки проекта LumeAI

## ✅ Текущее состояние (что уже работает)
- ✅ API Keys создание и удаление
- ✅ Базовая валидация ключей
- ✅ 69 AI моделей
- ✅ Function Calling
- ✅ Dashboard UI
- ✅ Swagger документация
- ✅ Аутентификация с сессиями

## 🔧 Требуется доработать

### 1. API Keys (OpenAI-совместимость) 🔑
**Текущие проблемы:**
- ❌ Нет поддержки `sk-` префикса как у OpenAI
- ❌ Нет детальной статистики использования
- ❌ Нет лимитов по времени (rate limiting)
- ❌ Нет информации о последнем использовании

**Что нужно добавить:**
- [ ] Генерация ключей с префиксом `sk-lumeai-`
- [ ] Счетчик запросов в реальном времени
- [ ] Rate limiting (requests per minute)
- [ ] Поле `last_used_at` в БД
- [ ] Поле `created_at` в БД
- [ ] Поддержка нескольких ключей на пользователя
- [ ] Endpoint для получения статистики ключа
- [ ] Автоматическая деактивация при превышении лимита

### 2. 2FA (Two-Factor Authentication) 🔐
**Текущие проблемы:**
- ❌ QR код генерируется но может не валидироваться
- ❌ Нет проверки при входе если 2FA включен
- ❌ Backup codes не реализованы

**Что нужно исправить:**
- [ ] Использовать `speakeasy` для генерации секретов
- [ ] Корректная генерация QR кода с `qrcode`
- [ ] Middleware для проверки 2FA при логине
- [ ] Генерация backup кодов
- [ ] Endpoint для отключения 2FA
- [ ] Endpoint для проверки статуса 2FA

### 3. Документация 📚
**Что обновить:**
- [ ] Swagger (api-docs) - актуальные endpoints
- [ ] API Endpoints страница - полный список
- [ ] Function Calling Guide - примеры с реальными ключами
- [ ] README.md - актуальная инструкция
- [ ] Создать QUICK_START.md
- [ ] Обновить примеры в коде

### 4. Endpoints для полной совместимости 🌐
**Добавить недостающие:**
- [ ] `GET /v1/models` - список моделей (OpenAI format)
- [ ] `GET /v1/models/:model` - информация о модели
- [ ] `POST /v1/embeddings` - если нужны embeddings
- [ ] `GET /dashboard` - главная страница dashboard
- [ ] `GET /api/v1/usage` - статистика использования
- [ ] `GET /api/v1/billing` - информация о биллинге

### 5. Улучшения UI 🎨
**Dashboard:**
- [ ] Реальная статистика использования API keys
- [ ] График использования по дням
- [ ] Показать префикс `sk-` в ключах
- [ ] Кнопка "Скопировать ключ" с индикацией
- [ ] Фильтрация моделей работает корректно
- [ ] Популярные модели реально популярные

### 6. Безопасность 🔒
- [ ] Хеширование API ключей в БД (bcrypt)
- [ ] Rate limiting на все endpoints
- [ ] CORS настройка
- [ ] Helmet.js для заголовков безопасности
- [ ] Валидация всех входных данных
- [ ] SQL injection protection
- [ ] XSS protection

### 7. Производительность ⚡
- [ ] Кэширование списка моделей
- [ ] Индексы в SQLite БД
- [ ] Gzip сжатие ответов
- [ ] Оптимизация запросов к БД
- [ ] Connection pooling

### 8. Логирование и мониторинг 📊
- [ ] Winston для логов
- [ ] Morgan для HTTP логов
- [ ] Логирование ошибок в файл
- [ ] Metrics endpoint для мониторинга

## 📋 Приоритеты

### High Priority (Сделать первым)
1. ✅ API Keys с `sk-` префиксом
2. ✅ Исправить 2FA полностью
3. ✅ Обновить документацию
4. ✅ Rate limiting

### Medium Priority
5. ⏳ UI улучшения Dashboard
6. ⏳ Дополнительные endpoints
7. ⏳ Безопасность (Helmet, CORS)

### Low Priority
8. ⏳ Производительность
9. ⏳ Расширенный мониторинг

## 🛠️ Технический план реализации

### Этап 1: API Keys с префиксом (30 мин)
```javascript
// Генерация ключа
function generateApiKey() {
    const randomPart = crypto.randomBytes(32).toString('hex');
    return `sk-lumeai-${randomPart}`;
}

// Хеширование перед сохранением
function hashApiKey(key) {
    return bcrypt.hashSync(key, 10);
}

// Валидация
function validateApiKey(key) {
    return key.startsWith('sk-lumeai-') && key.length === 73;
}
```

### Этап 2: Статистика ключей (20 мин)
```sql
ALTER TABLE api_keys ADD COLUMN last_used_at INTEGER;
ALTER TABLE api_keys ADD COLUMN created_at INTEGER;
ALTER TABLE api_keys ADD COLUMN requests_count INTEGER DEFAULT 0;

CREATE INDEX idx_api_keys_key ON api_keys(key);
CREATE INDEX idx_api_keys_user_id ON api_keys(user_id);
```

### Этап 3: 2FA исправление (30 мин)
```javascript
const speakeasy = require('speakeasy');
const QRCode = require('qrcode');

// Генерация секрета
const secret = speakeasy.generateSecret({
    name: 'LumeAI',
    issuer: 'LumeAI'
});

// QR код
const qrCode = await QRCode.toDataURL(secret.otpauth_url);

// Валидация
const verified = speakeasy.totp.verify({
    secret: user.twofa_secret,
    encoding: 'base32',
    token: code
});
```

### Этап 4: Rate Limiting (15 мин)
```javascript
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
    windowMs: 1 * 60 * 1000, // 1 минута
    max: 60, // 60 запросов
    message: 'Too many requests'
});

app.use('/api/', limiter);
```

### Этап 5: Обновление документации (40 мин)
- Swagger: обновить все schemas и примеры
- API Endpoints: добавить новые endpoints
- Function Calling: примеры с `sk-` ключами
- README: Quick Start секция

## 📝 Чек-лист финальной проверки

### API Keys
- [ ] Создание ключа возвращает `sk-lumeai-xxx`
- [ ] Ключ работает в Authorization header
- [ ] Статистика обновляется после каждого запроса
- [ ] Rate limiting срабатывает
- [ ] Деактивация ключа работает

### 2FA
- [ ] QR код генерируется корректно
- [ ] Сканирование QR кода в Google Authenticator работает
- [ ] Валидация 6-значного кода работает
- [ ] При входе с включенным 2FA требуется код
- [ ] Отключение 2FA требует код подтверждения

### Documentation
- [ ] Swagger показывает актуальные endpoints
- [ ] Примеры работают (можно скопировать и запустить)
- [ ] API Endpoints страница полная
- [ ] Function Calling примеры с реальными ключами
- [ ] README актуален

### UI/UX
- [ ] Dashboard загружается быстро
- [ ] Статистика отображается корректно
- [ ] Фильтры моделей работают
- [ ] Копирование ключа работает
- [ ] Нет ошибок в консоли браузера

### Security
- [ ] API ключи хешированы в БД
- [ ] Rate limiting настроен
- [ ] CORS разрешает только нужные домены
- [ ] Нет SQL injection
- [ ] XSS защита работает

## 🎯 Итоговая цель

Получить полностью функциональный API сервис уровня production с:
- ✅ OpenAI-совместимыми API ключами
- ✅ Надежной 2FA
- ✅ Полной документацией
- ✅ Безопасностью production-level
- ✅ Отличной производительностью
- ✅ Красивым Dashboard

## 📊 Оценка времени
- **Всего**: ~3-4 часа
- **High Priority**: 2 часа
- **Medium Priority**: 1 час
- **Low Priority**: 1 час

## 🚀 Начинаем!

**Текущий статус**: В процессе реализации
**Начало**: 05.11.2025 22:05
**Ожидаемое завершение**: 05.11.2025 23:30
