# ⚡ **Быстрый старт с Git**

## 🚀 **Первая настройка (один раз):**

### **1. Установи Git:**
```bash
winget install Git.Git
```

### **2. Настрой Git:**
```bash
git config --global user.name "Belnash3"
git config --global user.email "your-email@example.com"
```

### **3. Создай репозиторий на GitHub:**
- Открой: https://github.com/new
- Название: `LumeAI`
- Приватность: Private
- Нажми "Create repository"

### **4. Подключи проект к GitHub:**
```bash
cd d:\bukkit\rest-api

# Инициализируй Git
git init

# Добавь remote (замени USERNAME на свой)
git remote add origin https://github.com/Belnash3/LumeAI.git

# Первый коммит
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

---

## 📝 **Ежедневная работа:**

### **Вариант 1: Автоматический скрипт (рекомендуется)**

```powershell
# Загрузить на GitHub и обновить сервер
.\deploy.ps1 "Описание изменений"

# Примеры:
.\deploy.ps1 "Add new registration page"
.\deploy.ps1 "Fix login bug"
.\deploy.ps1 "Update styles"
```

### **Вариант 2: Быстрый коммит (без обновления сервера)**

```powershell
.\quick-commit.ps1 "Описание изменений"
```

### **Вариант 3: Вручную (3 команды)**

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

## 🔄 **Обновление сервера:**

### **Автоматически (через deploy.ps1):**
```powershell
.\deploy.ps1 "Update"
# Скрипт спросит, обновить ли сервер
```

### **Вручную:**
```bash
ssh YOUR_USERNAME@YOUR_SERVER_IP
cd ~/rest-api
git pull
./restart.sh app
```

### **Одной командой:**
```bash
ssh YOUR_USERNAME@YOUR_SERVER_IP "cd ~/rest-api && git pull && ./restart.sh app"
```

---

## 🎯 **Типичный рабочий процесс:**

```powershell
# Утро - начало работы
cd d:\bukkit\rest-api
git pull  # Скачать изменения (если работаешь с разных компьютеров)

# Работа над проектом
# ... редактируешь файлы ...

# Сохранение и деплой
.\deploy.ps1 "Add chat page"
# Скрипт автоматически:
# 1. Добавит файлы в Git
# 2. Создаст коммит
# 3. Загрузит на GitHub
# 4. Спросит, обновить ли сервер
# 5. Обновит сервер (если согласишься)
```

---

## 📋 **Полезные команды:**

```bash
# Посмотреть статус
git status

# Посмотреть изменения
git diff

# История коммитов
git log --oneline

# Отменить изменения в файле
git checkout -- filename.js

# Скачать изменения с GitHub
git pull
```

---

## 🔐 **Настройка SSH (опционально, для удобства):**

### **1. Создай SSH ключ:**
```bash
ssh-keygen -t ed25519 -C "your-email@example.com"
# Нажми Enter 3 раза (без пароля)
```

### **2. Скопируй ключ:**
```bash
cat ~/.ssh/id_ed25519.pub
```

### **3. Добавь на GitHub:**
- Открой: https://github.com/settings/keys
- Нажми "New SSH key"
- Вставь скопированный ключ
- Сохрани

### **4. Измени remote на SSH:**
```bash
git remote set-url origin git@github.com:Belnash3/LumeAI.git
```

Теперь не нужно вводить пароль при `git push`!

---

## 🐛 **Решение проблем:**

### **Ошибка: "Permission denied"**
```bash
# Создай Personal Access Token:
# https://github.com/settings/tokens
# Используй токен вместо пароля
```

### **Ошибка: "Merge conflict"**
```bash
# 1. Посмотри конфликты
git status

# 2. Открой файлы, найди <<<<<<< и >>>>>>>
# 3. Исправь вручную
# 4. Добавь и закоммить
git add .
git commit -m "Resolve conflict"
```

### **Забыл сделать pull перед push**
```bash
git pull --rebase
git push
```

---

## 📚 **Документация:**

- **Полное руководство:** `GIT_DEPLOY_GUIDE.md`
- **Git шпаргалка:** https://education.github.com/git-cheat-sheet-education.pdf

---

## ✅ **Готово!**

Теперь используй:
- `.\deploy.ps1 "message"` - для полного деплоя
- `.\quick-commit.ps1 "message"` - для быстрого коммита
- Или обычные git команды

**Удачи!** 🚀
