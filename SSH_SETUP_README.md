# 🔐 Настройка беспарольного доступа к серверу

## ⚡ Быстрый старт (2 минуты!)

### Шаг 1: Запусти автоматическую настройку

```powershell
.\setup-ssh-auto.ps1
```

Этот скрипт:
- ✅ Создаст SSH ключ
- ✅ Покажет команду для настройки сервера
- ✅ Скопирует команду в буфер обмена

### Шаг 2: Выполни команду (ОДИН раз с паролем)

Вставь скопированную команду и введи пароль **В ПОСЛЕДНИЙ РАЗ**

### Шаг 3: Готово! 🎉

Теперь используй БЕЗ пароля:
```powershell
.\deploy.ps1          # Автодеплой
ssh lumeai            # Подключение к серверу
```

---

## 📋 Что делает скрипт?

1. **Создает SSH ключ** в `~\.ssh\lumeai_key`
2. **Копирует публичный ключ** на сервер
3. **Настраивает SSH config** для удобного подключения

---

## ❌ Устранение проблем

### Проблема: "SSH ключ не найден"
**Решение:**
```powershell
.\setup-ssh-auto.ps1
```

### Проблема: "Cannot connect to server"
**Проверь:**
```powershell
ping YOUR_SERVER_IP    # Сервер доступен?
ssh lumeai             # SSH работает?
```

### Проблема: "Все равно спрашивает пароль"
**Решение:** Убедись что публичный ключ скопирован на сервер:
```bash
ssh YOUR_USERNAME@YOUR_SERVER_IP "cat ~/.ssh/authorized_keys"
```

Должен быть ключ, начинающийся с `ssh-rsa AAAA...`

---

## 🔧 Ручная настройка (если автомат не работает)

### 1. Создай ключ
```powershell
ssh-keygen -t rsa -b 4096 -f "$env:USERPROFILE\.ssh\lumeai_key" -N '""'
```

### 2. Скопируй на сервер
```bash
ssh - **Сервер**: `YOUR_SERVER_IP`
- **Пользователь**: `YOUR_USERNAME`
- **Пароль**: `YOUR_PASSWORD`

mkdir -p ~/.ssh
chmod 700 ~/.ssh
# Вставь содержимое из ~/.ssh/lumeai_key.pub
nano ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
exit
```

### 3. Проверь
```powershell
ssh -i "$env:USERPROFILE\.ssh\lumeai_key" YOUR_USERNAME@YOUR_SERVER_IP
```

---

## 🔒 Безопасность

### ✅ Что ПРАВИЛЬНО:
- SSH ключ хранится локально
- Приватный ключ защищен правами доступа
- Публичный ключ на сервере
- Пароль НЕ хранится в скриптах

### ❌ Что НЕПРАВИЛЬНО:
- ~~Хранить пароль в скриптах~~
- ~~Передавать пароль в открытом виде~~
- ~~Использовать одинаковый пароль везде~~

---

## 📊 Структура файлов

```
~\.ssh\
├── lumeai_key           # Приватный ключ (НЕ делись!)
├── lumeai_key.pub       # Публичный ключ (можно делиться)
└── config               # SSH конфигурация
```

---

## ✨ После настройки

Используй просто:
```powershell
# Деплой с автоматическим обновлением сервера
.\deploy.ps1 "My changes"

# Подключение к серверу
ssh lumeai

# Проверка статуса
ssh lumeai "pm2 status"

# Просмотр логов
ssh lumeai "pm2 logs lumeai"
```

**Пароль больше НЕ нужен!** 🎉

---

## 🚨 GitHub Actions предупреждения

Предупреждения о `SERVER_HOST`, `SERVER_USER`, `SERVER_SSH_KEY` - это нормально.

Они исчезнут после настройки секретов в GitHub:
1. Открой https://github.com/Belnsah3/Be1lnash3/settings/secrets/actions
2. Добавь секреты:
   - `SERVER_HOST` = `YOUR_SERVER_IP`
   - `SERVER_USER` = `YOUR_USERNAME`
   - `SERVER_SSH_KEY` = содержимое `~\.ssh\lumeai_key` (приватный ключ)

Или просто игнорируй - локальный деплой работает!

---

**Нужна помощь?** Проверь:
- ✅ Сервер онлайн: `ping YOUR_SERVER_IP`
- ✅ SSH работает: `ssh YOUR_USERNAME@YOUR_SERVER_IP`
- ✅ Ключ создан: `ls ~\.ssh\lumeai_key*`
